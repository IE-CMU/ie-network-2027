---
import { type Locale } from '@utils/locale'

interface Props {
  locale: Locale
}
const { locale } = Astro.props
---

<select value={locale} class="border border-gray-300 rounded-md p-2">
  <option value="en">English</option>
  <option value="th">Thai</option>
</select>
