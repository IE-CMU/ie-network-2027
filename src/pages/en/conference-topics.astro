---
import Layout from '@layouts/Layout.astro'
import ConferenceTopicsContent from '@components/pages/ConferenceTopicsContent.astro'
import { getLocale } from '@utils/locale'

// This page needs to be SSR becuase of the session data for the menu.
export const prerender = false

const locale = getLocale(Astro)
---

<Layout title="Conference Topics">
  <ConferenceTopicsContent locale={locale} />
</Layout>
